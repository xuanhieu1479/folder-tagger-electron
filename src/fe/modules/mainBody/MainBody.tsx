import React, { ReactElement } from 'react';
import './mainBodyStyle.scss';

const MainBody = (): ReactElement => {
  return <section className="main-body_container"></section>;
};

export default MainBody;
